'use strict';

const bookmarks = function() {

  const generateBookmarkElements = function(bookmark) {
    const filteredClass = bookmark.filtered ? 'bookmark_filtered' : '';
  }


  return {

  }
}());